load("//:ts.bzl", "ts_library", "ts_test")

ts_library(
    name = "decoders",
    srcs = ["line.ts"],
    visibility = ["//visibility:public"],
    deps = ["//src/internal/utils"],
)

ts_test(
    name = "decoders_test",
    srcs = ["line.test.ts"],
    tsconfig_types = ["node"],
    deps = [
        ":decoders",
        "//:node_modules/@types/node",
    ],
)
