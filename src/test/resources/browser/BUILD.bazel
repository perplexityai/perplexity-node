load("//:ts.bzl", "ts_test")

ts_test(
    name = "browser_test",
    srcs = ["sessions.test.ts"],
    tsconfig_types = ["node"],
    deps = [
        "//:node_modules/@types/node",
        "//src",
    ],
)
